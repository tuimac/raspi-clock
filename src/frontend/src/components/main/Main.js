import React from 'react';

import {
  Box,
  Button,
  Grid,
  Menu,
  MenuItem,
  Breadcrumbs,
  Link
} from '@mui/material';

class Main extends React.Component {

  constructor(props) {
    super(props);
    this.state = {
    };
  }

  componentDidMount() {
  }

  render() {
    return(
      <>
      </>
    );
  };
}

export default Main;
